const Command = require('../../structures/interactions/command')

module.exports = new Command({
    name: 'play',
    category: 'Music',
    description: 'Jouer une musique ou l\'ajouter à la file d\'attente.',
    async callback (client, interaction) {
        // PLAY A MUSIC A QUEUE IT
    }
})
